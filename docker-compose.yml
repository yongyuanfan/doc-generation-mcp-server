services:
  doc-generation-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "9103:9103"
    volumes:
      - ./templates:/app/templates
    restart: unless-stopped
